files:
  "/tmp/install_chrome.sh" :
    mode: "000755"
    owner: root
    group: root
    content: |
      #! /bin/bash
      curl https://intoli.com/install-google-chrome.sh | bash

commands:
  00_install_chrome:
    command: "sh /tmp/install_chrome.sh"
    ignoreErrors: false